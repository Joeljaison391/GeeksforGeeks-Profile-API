name: Deploy

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: akhileshns/heroku-deploy@v3.12.12 # This is the action
        with:
          heroku_api_key: HRKU-3d6bd7d9-265a-40d9-bb56-6927f39fc9f4
          heroku_app_name: "gfg-api" 
          heroku_email: "joeljaison394@proton.me"
          procfile: "web:node app.js"